//takes all the data written
	process.argv.splice(2, process.argv.length-1);

//makes the json file pretty
	null, 2

//if there is no error, pring a data json format
	if(!err){
	console.log(JSON.stringify(data));

// Take in the command line arguments
	var anything = process.argv;
